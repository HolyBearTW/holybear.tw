name: Update TMS radar reference

on:
  schedule:
    - cron: '20 19 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: update-tms-radar-reference
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate anonymized TMS radar curves
        env:
          VITE_NEXON_API_KEY: ${{ secrets.VITE_NEXON_API_KEY }}
        run: node scripts/generate-tms-radar-reference.mjs

      - name: Commit refreshed curves
        id: commit
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .vitepress/theme/maplestory/data/tmsRadarReference.json
          if git diff --cached --quiet; then
            echo "Radar reference is unchanged."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "chore: refresh TMS radar reference"
          git pull --rebase origin main
          git push origin HEAD:main
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Deploy refreshed static site
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run deploy-vitepress.yml --ref main
